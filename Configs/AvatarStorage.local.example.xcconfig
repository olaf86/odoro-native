// Copy this file to AvatarStorage.local.xcconfig and set your local bucket URL.
// Keep the $()/ separator so xcconfig does not treat // as a comment.
ODORO_AVATAR_STORAGE_BASE_URL = https:/$()/storage.googleapis.com/YOUR_BUCKET_NAME
